# Worked example: Code Architecture on a real task

A reference end-to-end session showing what the Code Architecture skill produces against a real Selr AI scenario. **You:** is the user, **Claude:** is the response.

---

## You

[A real task this skill is built for, kit-specific. Single sentence or short paragraph.]

## Claude

[Claude orients to the task in one or two sentences.]

> [Tool] Reads inputs the skill needs (file paths, intake brief, corpus, etc.). Confirms the kit's preconditions.

### Step 1: [first concrete action this skill performs]

[Concrete output of step 1. Not a generic description, a real result.]

### Step 2: [second concrete action]

[Concrete output of step 2.]

### Step 3: [third concrete action]

[Concrete output of step 3.]

### Final artifact

[The deliverable: file path, structured output, ranked list, recommendation, code snippet. Whatever the skill actually produces.]

---

## What this transcript proves

- The skill performs a concrete, named sequence (not free-form prose).
- The output is a real artifact, not a description of an artifact.
- The user's input was specific, the output is specific. No buzzword laundering.
- Edge cases the skill handles are named explicitly so an outsider can replicate.

Session captured against a real Selr AI scenario in May 2026.
