# Fixture: a bootstrap body with NO venue-wifi narration contract

This deliberately bad body carries the anchors but none of the narration
properties — no preflight, no frozen-warning sentence, no timeout guidance, no
after-the-fact confirmation. Every install-narration detector must fire on it.

I am setting up my Claude Code AI Business Assistant with Selr AI.

1. Install Node quietly.

       curl -o- https://example.invalid/install.sh | bash

2. Get the kit. Run the command silently and move on.

3. Copy files around.

Talk to me like I am not technical. Plain English, one step at a time.
